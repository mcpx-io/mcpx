{
  "name": "@mcpx-io/proxy",
  "version": "1.0.3",
  "type": "module",
  "description": "Proxy local HTTP que resolve secrets mcpx:enc:* antes de encaminhar para mcpx.online",
  "bin": {
    "mcpx-proxy": "dist/server.js"
  },
  "files": ["dist"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx server.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
