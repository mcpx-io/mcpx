{
  "name": "@mcpx-io/google-sheets",
  "version": "2.0.0",
  "type": "commonjs",
  "description": "MCP local para Google Sheets — criar, ler, escrever, formatar planilhas via service account",
  "bin": {
    "mcpx-google-sheets": "dist/server.js"
  },
  "files": ["dist"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsx server.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "google-auth-library": "^9.0.0",
    "googleapis": "^128.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
