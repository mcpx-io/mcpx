{
  "name": "@mcpx/redis",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for Redis — session-based, connection pooling, 20 tools — https://mcpx.online/redis/mcp",
  "scripts": {
    "build": "tsup server.ts --format esm --outDir dist --no-splitting --external ioredis",
    "start": "node dist/server.js",
    "dev": "tsx server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^17.3.1",
    "express": "^4.22.1",
    "ioredis": "^5.3.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  }
}
