name: Publish @mcpx/debug

on:
  push:
    tags:
      - "debug@*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
          scope: "@mcpx-io"

      - name: Install dependencies
        run: npm install --workspace=cli/debug

      - name: Build
        run: npm run build --workspace=cli/debug

      - name: Publish to GitHub Packages
        run: npm publish --workspace=cli/debug --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
