name: Publish @mcpx-io/apps-script

on:
  push:
    tags:
      - "apps-script@*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
          scope: "@mcpx-io"

      - name: Install dependencies
        run: npm install --workspace=cli/apps-script --include=dev

      - name: Build
        run: npm run build --workspace=cli/apps-script

      - name: Publish to GitHub Packages
        run: npm publish --workspace=cli/apps-script --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
